Algoritmo ejercicio3
	//Realizar un algoritmo que pida números (se pedirá por teclado la cantidad de números a introducir). El
	//programa debe informar de cuantos números introducidos son mayores que 0, menores que 0 e
	//iguales a 0.
	contadorPositivo = 0
    contadorNegativo = 0
    contadorCero = 0
	
    Escribir "Ingrese la cantidad de números a introducir:"
    Leer cantidad
	
    Para i desde 1 hasta cantidad Hacer
        Escribir "Ingrese un número:"
        Leer num
		
        Si num > 0 Entonces
            contadorPositivo = contadorPositivo + 1
        Sino Si num < 0 Entonces
				contadorNegativo = contadorNegativo + 1
			Sino
				contadorCero = contadorCero + 1
			Fin Si
		FinSi
	FinPara

	Escribir "Cantidad de números mayores que 0: ", contadorPositivo
	Escribir "Cantidad de números menores que 0: ", contadorNegativo
	Escribir "Cantidad de números iguales a 0: ", contadorCero
FinAlgoritmo
